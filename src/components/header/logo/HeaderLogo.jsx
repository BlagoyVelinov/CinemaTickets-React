import { Link } from "react-router";

export default function HeaderLogo() {
    return (
        <div className="fleft">
            <Link to="/">Cinema <span>Tickets</span>
            </Link>
        </div>
    );
}